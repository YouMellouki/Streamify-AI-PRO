# Add this to your docker-compose.yml
services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: always

  redisinsight:
    image: redis/redisinsight:latest
    ports:
      - "5540:5540"
    restart: always

volumes:
  redis-data: